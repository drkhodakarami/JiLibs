version = getSubprojectVersion(project)

repositories {
    maven {
        url = "https://cursemaven.com"
        content {
            includeGroup "curse.maven"
        }
    }
}

loom {
    accessWidenerPath = file('src/main/resources/jilibs_energy.accesswidener')
}

moduleDependencies(project, [
        'jilibs-base',
        'jilibs-inventory',
])

dependencies{
    // Energy API Handling Dependency ================================================================================================================
    api("teamreborn:energy:${rootProject.energy_version}") {
        exclude(group: "net.fabricmc.fabric-api")
    }
}