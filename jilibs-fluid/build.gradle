version = getSubprojectVersion(project)

loom {
    accessWidenerPath = file('src/main/resources/jilibs-fluid.accesswidener')
}

moduleDependencies(project, [
        'jilibs-base',
        'jilibs-inventory',
])