version = getSubprojectVersion(project)

loom {
    accessWidenerPath = file('src/main/resources/jilibs_fluid.accesswidener')
}

moduleDependencies(project, [
        'jilibs-base',
        'jilibs-inventory',
])